<Window x:Class="Cryptunnel.App.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="关于 Cryptunnel" Height="380" Width="520"
        Background="#080a0f" Foreground="#eef2f8"
        FontFamily="Segoe UI, Microsoft YaHei, sans-serif"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None" AllowsTransparency="False">
    <Window.Resources>
        <SolidColorBrush x:Key="Tx1" Color="#eef2f8"/>
        <SolidColorBrush x:Key="Tx2" Color="#9aa5bb"/>
        <SolidColorBrush x:Key="Tx3" Color="#5a6680"/>
        <SolidColorBrush x:Key="Accent" Color="#5b9cff"/>
        <SolidColorBrush x:Key="Accent2" Color="#8b6cff"/>
        <SolidColorBrush x:Key="Danger" Color="#ff5d6c"/>
        <SolidColorBrush x:Key="Hair" Color="#ffffff" Opacity="0.10"/>
        <SolidColorBrush x:Key="Hair2" Color="#ffffff" Opacity="0.18"/>
        <SolidColorBrush x:Key="InputBorder" Color="#ffffff" Opacity="0.12"/>
        <SolidColorBrush x:Key="InputBg" Color="#1c2230"/>

        <LinearGradientBrush x:Key="WinBg" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#0b0e16" Offset="0"/>
            <GradientStop Color="#070a10" Offset="1"/>
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="AccentGrad" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#5b9cff"/><GradientStop Color="#8b6cff" Offset="1"/>
        </LinearGradientBrush>

        <Style TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="{StaticResource Tx1}"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="FontSize" Value="12.5"/>
        </Style>

        <Style x:Key="PrimaryBtn" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentGrad}"/>
            <Setter Property="Foreground" Value="#ffffff"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="28,9"/>
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="9">
                            <Border.Effect>
                                <DropShadowEffect Color="#5b9cff" BlurRadius="18" ShadowDepth="0" Opacity="0.45"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#7aaeff"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DlgBtn" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource InputBg}"/>
            <Setter Property="Foreground" Value="{StaticResource Tx1}"/>
            <Setter Property="BorderBrush" Value="{StaticResource Hair2}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16,7"/>
            <Setter Property="MinWidth" Value="84"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource Accent}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Border Background="{StaticResource WinBg}"/>
        <Ellipse Width="380" Height="380" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,-180,-120,0" Opacity="0.22">
            <Ellipse.Fill>
                <RadialGradientBrush>
                    <GradientStop Color="#8b6cff" Offset="0"/>
                    <GradientStop Color="#008b6cff" Offset="1"/>
                </RadialGradientBrush>
            </Ellipse.Fill>
        </Ellipse>
        <Ellipse Width="260" Height="260" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="-90,90,0,0" Opacity="0.14">
            <Ellipse.Fill>
                <RadialGradientBrush>
                    <GradientStop Color="#5b9cff" Offset="0"/>
                    <GradientStop Color="#005b9cff" Offset="1"/>
                </RadialGradientBrush>
            </Ellipse.Fill>
        </Ellipse>

        <Grid Margin="22">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 标题栏 -->
            <Border Grid.Row="0" Height="40" Background="Transparent" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Width="28" Height="28" CornerRadius="8">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#8b6cff"/><GradientStop Color="#5b9cff" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border.Effect>
                                <DropShadowEffect Color="#5b9cff" BlurRadius="16" ShadowDepth="0" Opacity="0.6"/>
                            </Border.Effect>
                            <TextBlock Text="J" Foreground="#ffffff" FontWeight="Bold" FontSize="15" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Text="关于 Cryptunnel" VerticalAlignment="Center" Margin="12,0,0,0" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource Tx1}"/>
                    </StackPanel>
                    <Button Grid.Column="1" Width="30" Height="26" Background="Transparent" BorderThickness="0" Foreground="{StaticResource Tx2}" FontSize="13" Cursor="Hand" Click="BtnClose_Click" Content="✕">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="8"><ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{StaticResource Danger}"/>
                                        <Setter Property="Foreground" Value="#ffffff"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>

            <!-- 卡片 -->
            <Border Grid.Row="1" CornerRadius="16" Padding="26,20" Margin="0,14,0,0" BorderThickness="1" BorderBrush="{StaticResource Hair}">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#141a26" Offset="0"/>
                        <GradientStop Color="#10141d" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Border.Effect>
                    <DropShadowEffect Color="#000000" BlurRadius="36" ShadowDepth="0" Opacity="0.5"/>
                </Border.Effect>

                <StackPanel>
                    <!-- 顶部品牌区 -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,18">
                        <Border Width="56" Height="56" CornerRadius="14">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#8b6cff"/><GradientStop Color="#5b9cff" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border.Effect>
                                <DropShadowEffect Color="#5b9cff" BlurRadius="24" ShadowDepth="0" Opacity="0.65"/>
                            </Border.Effect>
                            <TextBlock Text="J" Foreground="#ffffff" FontWeight="Bold" FontSize="28" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Margin="16,0,0,0" VerticalAlignment="Center">
                            <TextBlock x:Name="AppNameText" Text="Cryptunnel 隧道客户端" FontSize="17" FontWeight="SemiBold" Foreground="{StaticResource Tx1}"/>
                            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                <TextBlock Text="v" FontSize="11.5" Foreground="{StaticResource Tx3}" VerticalAlignment="Center"/>
                                <TextBlock x:Name="VersionText" Text="1.2.6" FontSize="11.5" Foreground="{StaticResource Accent}" FontFamily="Consolas, Courier New" VerticalAlignment="Center"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>

                    <!-- 分隔线 -->
                    <Border Height="1" Background="{StaticResource Hair}" Margin="0,0,0,16"/>

                    <!-- 配置目录 -->
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="88"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="配置目录" Foreground="{StaticResource Tx2}"/>
                        <Border Grid.Column="1" Background="{StaticResource InputBg}" BorderBrush="{StaticResource InputBorder}" BorderThickness="1" CornerRadius="8" Padding="11,7">
                            <TextBlock x:Name="ConfigDirText" Text="C:\Users\..." Foreground="{StaticResource Tx1}" FontFamily="Consolas, Courier New" FontSize="11.5" TextTrimming="CharacterEllipsis" ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"/>
                        </Border>
                    </Grid>

                    <!-- 日志目录 -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="88"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="日志目录" Foreground="{StaticResource Tx2}"/>
                        <Border Grid.Column="1" Background="{StaticResource InputBg}" BorderBrush="{StaticResource InputBorder}" BorderThickness="1" CornerRadius="8" Padding="11,7">
                            <TextBlock x:Name="LogDirText" Text="C:\Users\..." Foreground="{StaticResource Tx1}" FontFamily="Consolas, Courier New" FontSize="11.5" TextTrimming="CharacterEllipsis" ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"/>
                        </Border>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- 操作栏 -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                <Button Content="关闭" Click="BtnClose_Click" Style="{StaticResource PrimaryBtn}" IsDefault="True" IsCancel="True"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>